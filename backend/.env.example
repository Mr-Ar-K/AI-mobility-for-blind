# Backend Environment Configuration

# Database Configuration
# PostgreSQL connection string format: postgresql://username:password@host:port/database_name
# Replace YOUR_PASSWORD with your actual PostgreSQL password
# Database includes user language preferences (en, te, hi) for multi-language TTS
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost/ai_mobility_db

# Custom YOLOv8n Model Path
# Single model detects: Car, Person, Green Light, zebra crossing
# Model should be approximately 6-10 MB
MODEL_PATH=models/Yolov8n.pt

# Backend Server Configuration
# Use localhost for local development
# Backend supports async video processing with real-time progress updates
Backend_PORT=http://localhost:8000
Backend_PORT_FALLBACK=http://localhost:8000

# Storage Directories
# These directories will be created automatically
# Files organized by: username/date/time/
HISTORY_STORAGE_DIR=storage/history
TEMP_UPLOAD_DIR=tmp/

# Multi-Language Support
# Supported languages: en (English), te (Telugu), hi (Hindi)
# Audio translations are complete native translations, not just accents
# Users can set language preference in profile or override per upload

# Video Encoding
# Videos encoded with H.264 High Profile Level 4.1 for optimal browser compatibility
# Supports up to 1920x1080@30fps without warnings
# imageio-ffmpeg used for encoding (install: pip install imageio-ffmpeg)

# Audio Settings
# gTTS (Google Text-to-Speech) requires internet connection
# Audio files generated with complete language translations
# Playback speed adjustable from 0.5x to 2x in frontend
